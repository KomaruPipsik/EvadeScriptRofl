local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer

-- ==========================================
-- Создание интерфейса (Меню)
-- ==========================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CustomPlayerMenu"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = player:WaitForChild("PlayerGui")

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 250, 0, 230)
MainFrame.Position = UDim2.new(0.5, -125, 0.5, -115)
MainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.ZIndex = 1
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 30)
Title.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
Title.BorderSizePixel = 0
Title.Text = "Player Menu"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 14
Title.ZIndex = 2
Title.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 8)
TitleCorner.Parent = Title

local TitleCover = Instance.new("Frame")
TitleCover.Size = UDim2.new(1, 0, 0, 8)
TitleCover.Position = UDim2.new(0, 0, 1, -8)
TitleCover.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
TitleCover.BorderSizePixel = 0
TitleCover.ZIndex = 2
TitleCover.Parent = Title

-- Кнопка 1: Переворот
local FlipButton = Instance.new("TextButton")
FlipButton.Size = UDim2.new(1, -20, 0, 40)
FlipButton.Position = UDim2.new(0, 10, 0, 45)
FlipButton.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
FlipButton.Text = "Переворот: ВЫКЛ"
FlipButton.TextColor3 = Color3.fromRGB(255, 100, 100)
FlipButton.Font = Enum.Font.GothamSemibold
FlipButton.TextSize = 14
FlipButton.ZIndex = 5
FlipButton.Active = true
FlipButton.Parent = MainFrame

local FlipCorner = Instance.new("UICorner")
FlipCorner.CornerRadius = UDim.new(0, 6)
FlipCorner.Parent = FlipButton

-- Кнопка 2: Инерция
local SpeedButton = Instance.new("TextButton")
SpeedButton.Size = UDim2.new(1, -20, 0, 40)
SpeedButton.Position = UDim2.new(0, 10, 0, 95)
SpeedButton.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
SpeedButton.Text = "Инерция: ВЫКЛ"
SpeedButton.TextColor3 = Color3.fromRGB(255, 100, 100)
SpeedButton.Font = Enum.Font.GothamSemibold
SpeedButton.TextSize = 14
SpeedButton.ZIndex = 5
SpeedButton.Active = true
SpeedButton.Parent = MainFrame

local SpeedCorner = Instance.new("UICorner")
SpeedCorner.CornerRadius = UDim.new(0, 6)
SpeedCorner.Parent = SpeedButton

-- Кнопка 3: Ходьба по стенам и потолку
local WallWalkButton = Instance.new("TextButton")
WallWalkButton.Size = UDim2.new(1, -20, 0, 40)
WallWalkButton.Position = UDim2.new(0, 10, 0, 145)
WallWalkButton.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
WallWalkButton.Text = "Стены/Потолок: ВЫКЛ"
WallWalkButton.TextColor3 = Color3.fromRGB(255, 100, 100)
WallWalkButton.Font = Enum.Font.GothamSemibold
WallWalkButton.TextSize = 14
WallWalkButton.ZIndex = 5
WallWalkButton.Active = true
WallWalkButton.Parent = MainFrame

local WallWalkCorner = Instance.new("UICorner")
WallWalkCorner.CornerRadius = UDim.new(0, 6)
WallWalkCorner.Parent = WallWalkButton

-- ==========================================
-- Логика перетаскивания меню (Drag & Drop)
-- ==========================================
local dragging = false
local dragInput, mousePos, framePos

Title.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		mousePos = input.Position
		framePos = MainFrame.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

Title.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
		dragInput = input
	end
end)

RunService.RenderStepped:Connect(function()
	if dragging and dragInput then
		local delta = dragInput.Position - mousePos
		MainFrame.Position = UDim2.new(framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale, framePos.Y.Offset + delta.Y)
	end
end)

-- ==========================================
-- Переменные и функции
-- ==========================================
local isFlipped = false
local keepSpeed = false
local isWallWalking = false

local savedVelocity = Vector3.zero
local originalC0 = nil
local lastChar = nil

local rayParams = RaycastParams.new()
rayParams.FilterType = Enum.RaycastFilterType.Exclude

local function getRootJoint(character)
	if not character then return nil end
	local hrp = character:FindFirstChild("HumanoidRootPart")
	if hrp and hrp:FindFirstChild("RootJoint") then
		return hrp.RootJoint -- R6
	end
	local lowerTorso = character:FindFirstChild("LowerTorso")
	if lowerTorso and lowerTorso:FindFirstChild("Root") then
		return lowerTorso.Root -- R15
	end
	return nil
end

-- Кнопка 1: Переворот
FlipButton.MouseButton1Click:Connect(function()
	isFlipped = not isFlipped
	if isFlipped then
		FlipButton.Text = "Переворот: ВКЛ"
		FlipButton.TextColor3 = Color3.fromRGB(100, 255, 100)
	else
		FlipButton.Text = "Переворот: ВЫКЛ"
		FlipButton.TextColor3 = Color3.fromRGB(255, 100, 100)
		
		local char = player.Character
		local joint = getRootJoint(char)
		if joint and originalC0 then
			joint.C0 = originalC0
		end
	end
end)

-- Кнопка 2: Инерция
SpeedButton.MouseButton1Click:Connect(function()
	keepSpeed = not keepSpeed
	if keepSpeed then
		SpeedButton.Text = "Инерция: ВКЛ"
		SpeedButton.TextColor3 = Color3.fromRGB(100, 255, 100)
	else
		SpeedButton.Text = "Инерция: ВЫКЛ"
		SpeedButton.TextColor3 = Color3.fromRGB(255, 100, 100)
		savedVelocity = Vector3.zero
	end
end)

-- Кнопка 3: Стенолазание
WallWalkButton.MouseButton1Click:Connect(function()
	isWallWalking = not isWallWalking
	if isWallWalking then
		WallWalkButton.Text = "Стены/Потолок: ВКЛ"
		WallWalkButton.TextColor3 = Color3.fromRGB(100, 255, 100)
	else
		WallWalkButton.Text = "Стены/Потолок: ВЫКЛ"
		WallWalkButton.TextColor3 = Color3.fromRGB(255, 100, 100)
	end
end)

-- Исходная функция проверки поверхностей для Wall Walk
local function checkSurfaces(char)
	local hrp = char:FindFirstChild("HumanoidRootPart")
	if not hrp then return nil end

	rayParams.FilterDescendantsInstances = {char}

	local checkDirections = {
		-hrp.CFrame.UpVector * 5,
		hrp.CFrame.LookVector * 4,
		-hrp.CFrame.LookVector * 4,
		hrp.CFrame.RightVector * 4,
		-hrp.CFrame.RightVector * 4,
		hrp.CFrame.UpVector * 5
	}

	for _, dir in ipairs(checkDirections) do
		local result = workspace:Raycast(hrp.Position, dir, rayParams)
		if result and result.Instance and result.Instance.CanCollide then
			return result.Normal
		end
	end
	return nil
end

-- ==========================================
-- Игровые циклы
-- ==========================================

-- Цикл 1: Переворот персонажа (RenderStepped)
RunService.RenderStepped:Connect(function()
	local char = player.Character
	if not char then return end
	
	-- При смене персонажа / респавне сбрасываем сохраненный C0
	if char ~= lastChar then
		lastChar = char
		originalC0 = nil
	end
	
	local joint = getRootJoint(char)
	if joint then
		if not originalC0 then
			originalC0 = joint.C0
		end
		
		if isFlipped then
			joint.C0 = originalC0 * CFrame.Angles(0, 0, math.pi)
		else
			joint.C0 = originalC0
		end
	end
end)

-- Цикл 2: Физика (Инерция и Стенолазание в Stepped перед шагом физики)
RunService.Stepped:Connect(function(_, dt)
	local char = player.Character
	if not char then return end
	local hrp = char:FindFirstChild("HumanoidRootPart")
	local hum = char:FindFirstChildOfClass("Humanoid")
	if not hrp or not hum or hum.Health <= 0 then return end

	-- Инерция
	if keepSpeed then
		if hum.MoveDirection.Magnitude > 0.1 then
			-- Запоминаем скорость, пока зажаты клавиши
			savedVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, 0, hrp.AssemblyLinearVelocity.Z)
		else
			-- Применяем сохраненную скорость, когда клавиши отжаты
			if savedVelocity.Magnitude > 0.5 then
				hrp.AssemblyLinearVelocity = Vector3.new(savedVelocity.X, hrp.AssemblyLinearVelocity.Y, savedVelocity.Z)
			end
		end
	end

	-- Исходная логика стенолазания
	if isWallWalking then
		local normal = checkSurfaces(char)
		if normal then
			local currentLook = hrp.CFrame.LookVector
			local right = currentLook:Cross(normal)
			if right.Magnitude > 0.001 then
				local forward = normal:Cross(right).Unit
				hrp.CFrame = CFrame.fromMatrix(hrp.Position, right.Unit, normal, -forward)
			end

			local antiGravity = Vector3.new(0, workspace.Gravity * dt, 0)
			local stickForce = -normal * 25 * dt
			hrp.AssemblyLinearVelocity = hrp.AssemblyLinearVelocity + antiGravity + stickForce
		end
	end
end)
